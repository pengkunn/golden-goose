import { _decorator, Component, Node } from 'cc';
import { View } from '../components/view/View';
const { ccclass, property } = _decorator;

@ccclass('FeedGooseView')
export class FeedGooseView extends View {
    start() {

    }

    update(deltaTime: number) {
        
    }
}


